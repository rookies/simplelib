#include "Random.hh"

int Random::getNumber(int *result, int min, int max) {
    // check range, max has to be greater than min
    if (max <= min) return -1;

    /* TODO */
    *result = 43;
    return 0;
}